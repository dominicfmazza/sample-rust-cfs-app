# Sample RUST CFS App 

This is based on the good work in the forked repository above. It was not 
shown to be bound into cFS, just that the bindings were generated, but this is a functional 
cFS application in Rust that compiles and runs.
